version: "2"

options:
  verbose: true
syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  php-sync: # tip: add -sync and you keep consistent names as a convention
    src: './'
    sync_userid: '82'
    sync_excludes: ['.idea/', '.git/', 'node_modules/']
  webpack-sync: # tip: add -sync and you keep consistent names as a convention
    src: './'
    sync_userid: '82'
    sync_excludes: ['.idea/', '.git/', 'node_modules/']