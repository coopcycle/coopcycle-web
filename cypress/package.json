{
  "name": "e2e",
  "version": "1.0.0",
  "description": "Cypress only dependencies for running e2e tests on CI",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.4.1",
    "dotenv": "^8.6.0"
  }
}
