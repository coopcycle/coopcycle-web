apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minio-dev.fullname" . }}-test-connection"
  labels:
    {{- include "minio-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minio-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
