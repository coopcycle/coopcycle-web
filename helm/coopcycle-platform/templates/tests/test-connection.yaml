apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coopcycle-platform.fullname" . }}-test-connection"
  labels:
    {{- include "coopcycle-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coopcycle-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
