apiVersion: v1
kind: Service
metadata:
  name: {{ include "mjml.fullname" . }}
  labels:
    {{- include "mjml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mjml.selectorLabels" . | nindent 4 }}
