apiVersion: v1
kind: Service
metadata:
  name: {{ include "osrm.fullname" . }}
  labels:
    {{- include "osrm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osrm.selectorLabels" . | nindent 4 }}
