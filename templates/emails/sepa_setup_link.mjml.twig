{% extends 'emails/layout.mjml.twig' %}

{% block content %}
  <mj-text align="left" line-height="20px">
    {{ 'admin.send_sepa_setup_link.body' | trans({ '%name%': payer_name }, 'emails') | raw }}
  </mj-text>
  <mj-button href="{{ setup_url }}" background-color="{{ coopcycle_theme_color('primary', '#10ac84') }}" color="{{ coopcycle_theme_color('primary-content', 'white') }}" font-family="Raleway, Arial, sans-serif">
    {{ 'admin.send_sepa_setup_link.cta' | trans({}, 'emails') }}
  </mj-button>
{% endblock %}
