{% extends 'emails/layout.mjml.twig' %}

{% block content %}
<mj-column>
  <mj-text align="left" line-height="20px">
  {{ 'order.cancelled.body.intro' | trans({}, 'emails') | raw }}
  {% if order.hasVendor() %}
  <br>
  <br>
  {{ 'order.foodtech.cancelled' | trans({ '%order.number%': order.number }, 'emails') | raw }}
  {% endif %}
  </mj-text>
</mj-column>
{% endblock %}
