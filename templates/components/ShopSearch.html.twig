{# Sort cuisines alphabetically #}
{% set cuisines = [] %}
{% for cuisine in this.cuisines %}
  {% set cuisines = cuisines|merge([{
    label: cuisine.name|trans({}, 'cuisines'),
    value: cuisine.name,
    icon: cuisine.hasIcon() ? cuisine.icon : 'fluent-color:search-sparkle-16'
  }]) %}
{% endfor %}
{% set cuisines = cuisines|sort((a, b) => a.label <=> b.label) %}

<div {{ attributes.defaults(stimulus_controller('shop-search')) }}>

  <div class="mb-4">

    <div class="swiper filters categories mb-4">
      <ul class="swiper-wrapper">
        <li class="swiper-slide">
          <a class="py-2 px-4 cursor-pointer {% if this.category is empty and this.type is empty %}font-semibold{% endif %}" href="{{ path('shops') }}">
            <i class="fa fa-lg fa-home" aria-hidden="true"></i>
          </a>
        </li>
        {% if this.types|length > 1 %}
        {% for type in this.types %}
        <li class="swiper-slide">
          <a class="py-2 px-4 cursor-pointer {% if this.type == type|local_business_type_key %}font-semibold{% endif %}" {{ live_action('setType', { type: type|local_business_type_key }) }}>{{ type|local_business_type }}</a>
        </li>
        {% endfor %}
        {% endif %}
        {% for category in this.categories %}
        <li class="swiper-slide">
          <a class="py-2 px-4 cursor-pointer {% if category.key == this.category %}font-semibold{% endif %}" {{ live_action('setCategory', { key: category.key }) }}>{{ category.transKey|trans }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>

    <div class="swiper filters cuisines">
      <ul class="swiper-wrapper">
      {% for cuisine in cuisines %}
        <li class="swiper-slide">
          <a class="flex flex-col items-center py-2 px-4 cursor-pointer {% if cuisine.value in this.cuisine %}bg-amber-50{% endif %}" {{ live_action('toggleCuisine', { name: cuisine.value }) }}>
            <span class="mb-1">
              {{ ux_icon(cuisine.icon, { style: 'font-size: 30px' }) }}
            </span>
            <small>{{ cuisine.label }}</small>
          </a>
        </li>
      {% endfor %}
      </ul>
      <div class="swiper-button-prev bg-gray-200"></div>
      <div class="swiper-button-next bg-gray-200"></div>
    </div>
  </div>

  <div class="mx-auto px-4">
    {% if this.shops|length == 0 %}
      <div class="alert alert-warning text-center">
        {{ 'restaurants.no_results'|trans|raw }}
      </div>
    {% else %}
      {# https://symfony.com/bundles/ux-live-component/current/index.html#skipping-updating-certain-elements #}
      {# To force an ignored element to re-render, give its parent element an id attribute. #}
      {# During a re-render, if this value changes, all of the children of the element will be re-rendered, even those with data-live-ignore. #}
      <div id="results-{{ this.cacheKey }}" class="grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4 restaurant-list mb-4">
        {% if page > 1 %}
          {# 🦊 #}
          {# A fake "previous page" div per already-loaded page is needed to trick the system #}
          {# Each must have the same ID as its original page so morphdom keeps them via data-live-ignore #}
          {% for i in 1..page - 1 %}
            <div id="page--{{ i }}" data-live-ignore="true"></div>
          {% endfor %}
        {% endif %}
        {# https://caniuse.com/css-display-contents #}
        <div id="page--{{ page }}" data-live-ignore="true" class="contents">
          {% include "_partials/restaurant/shops_list.html.twig" with { restaurants: this.shops } %}
        </div>
      </div>
      <div class="text-center my-4" data-loading="show" style="display: none;">
        <i class="fa fa-spinner fa-spin fa-2x" aria-hidden="true"></i>
      </div>
      {% if this.hasMore %}
        {# Sentinel: when it scrolls into view the IntersectionObserver fires the appear event,  #}
        {# which is routed to the live component's `more` action. Kept outside the grid div so   #}
        {# morphdom's ID-based diffing on the grid is not disturbed by the appear controller.    #}
        <div {{ stimulus_controller('appear') }}>
          <div
            data-appear-target="loader"
            data-action="appear->live#action"
            data-live-action-param="debounce(750)|more"
          ></div>
        </div>
      {% endif %}
    {% endif %}
  </div>
</div>
