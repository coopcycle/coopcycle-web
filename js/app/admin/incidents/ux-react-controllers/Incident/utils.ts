import {
  money as _money,
  weight as _weight,
} from '../../../../utils/format'

export function money(amount) {
  return _money(amount)
}

export function weight(amount) {
  return _weight(amount)
}
