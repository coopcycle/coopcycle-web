- hosts: all
  become: yes
  roles:
    - init
    - nginx
    - ANXS.postgresql
    - geerlingguy.php
    - DavidWittman.redis # Failed to reload sysctl: sysctl: permission denied on key 'vm.overcommit_memory
    - redis-tools
    - nodejs
    - { role: alexsegura.pm2, become: no }
    - osrm
