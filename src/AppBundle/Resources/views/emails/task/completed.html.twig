{% extends '@App/emails/layout.html.twig' %}

{% block content %}

  {% if task.done %}
    {{ 'task.done.body' | trans({
      '%task.id%': task.id,
    }, 'emails') | raw }}
  {% endif %}

  {% if task.failed %}
    {{ 'task.failed.body' | trans({
      '%task.id%': task.id,
    }, 'emails') | raw }}
  {% endif %}

{% endblock %}
