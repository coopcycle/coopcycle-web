{{ 'order.summary.heading'|trans({ '%number%': order.number, '%id%': order.id }) }}

---

{% for item in order.items %}
{{ item.variant.product.name }} × {{ item.quantity }}
{% for adjustment in item.getAdjustments('menu_item_modifier') %}
  - {{ adjustment.label }}
{% endfor %}
{% for adjustment in item.getAdjustments('reusable_packaging') %}
  - {{ adjustment.label }}
{% endfor %}

{% endfor %}
