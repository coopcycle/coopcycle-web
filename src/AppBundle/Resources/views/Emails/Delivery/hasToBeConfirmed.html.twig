{% extends '@App/Emails/layout.html.twig' %}

{% block content %}
  {{ 'delivery.has_to_be_confirmed.body' | trans({
    '%order_url%': url('admin_order', { id: order.id, type: 'sylius' }),
  }, 'emails') | raw }}
{% endblock %}
