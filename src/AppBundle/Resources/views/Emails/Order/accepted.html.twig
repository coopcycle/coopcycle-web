{% extends '@App/Emails/layout.html.twig' %}

{% block content %}
  {{ 'order.accepted.body.intro' | trans({}, 'emails') | raw }}
  <br>
  <br>
  {% if order.foodtech %}
  {{ 'order.foodtech.accepted' | trans({}, 'emails') | raw }}
  {% else %}
  {{ 'order.public_url' | trans({
    '%order.public_url%': url('public_order', { number: order.number }),
  }, 'emails') | raw }}
  {% endif %}
{% endblock %}
