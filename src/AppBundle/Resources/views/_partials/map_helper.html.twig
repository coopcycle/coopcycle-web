<script>
window.AppData = window.AppData || {}
window.AppData.MapHelper = {
  routeURL: "{{ path('routing_route', { coordinates: '__COORDINATES__' }) }}",
  center: "{{ coopcycle_setting('latlng') }}"
};
</script>
